# Test and development dependencies.
#
# Kept apart from requirements.txt so a deployment installs the runtime only. The
# runtime bounds in that file still apply here - install it first.
#
#     pip install -r requirements.txt -r requirements-dev.txt
#     pytest                    # full suite
#     pytest -m "not slow"      # skips anything needing models/ on disk

-r requirements.txt

pytest>=8.0
# Property-based testing. The example-based tests confirm the cases we thought of;
# these generate the ones we did not - which is how the all-NaN column that
# `split_feature_types` misclassified as binary was found.
hypothesis>=6.100
# FastAPI's TestClient is a thin wrapper over httpx and will not import without it.
httpx>=0.27
