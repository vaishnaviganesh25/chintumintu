[pytest]
testpaths = tests
addopts = -q --strict-markers --strict-config
markers =
    slow: needs the trained model artifacts on disk (skipped when they are absent)
filterwarnings =
    ignore::UserWarning
    ignore::DeprecationWarning
