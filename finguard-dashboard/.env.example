# Base URL of the FinGuard Module 4 API (see ../main.py).
# Copy this file to `.env.local` to point the dashboard at a different host or port.
# Defaults to http://localhost:8080 when unset.
VITE_API_BASE_URL=http://localhost:8080
